# 2026-09-20

## 43. A global initialised with an import's address (implemented at 178)

`p: :imp-fn` — a Red/System static initializer holding the address of an
imported function — used to die in codegen on both backends. It now works on
Windows-X86-64, Darwin-ARM64, Linux-X86-64 and Linux-ARM64.

Design: functions, globals and imports already share one reference-id space in
that order, so the initializer's reference rides in the slice the import's own
calls already use, distinguished by sign — positive = code offset to patch,
negative = 1-based position of a data slot for the loader to fill (the same
encoding `load-codegen` already uses for a data reference).

- ARM64: new arm in `valid-static-address-initializer?` + `target-id` via the
  new `static-address-target-id` helper (shared by its three call sites).
- x64: same, plus the reference must be *counted* in `measure-module-functions`
  under `argument-marking?`, or an import whose address is only taken gets no
  slice. `place-module-globals` just returns 0 for it.
- PE: an extra `IMAGE_IMPORT_DESCRIPTOR` per slot (library name reused,
  one-entry ILT + null, `FirstThunk` aimed at the slot) — there is no PE
  relocation that resolves a name after loading.
- ELF: one `R_*_GLOB_DAT` per slot in `.rela.dyn`, symbol index = 1-based
  position in the flat import list (= its `.dynsym` index).
- Mach-O: one extra bind opcode per slot at the data offset.

Baseline: `hybrid-compiler179.exe` (178->179, 6408192 bytes). Fixed point 179
vs 180 = the usual 17 bytes of clock noise. Red/System suite 10593 tests /
12680 assertions / 12680 passed / 0 failed / 0 compile-failures.

### The PE rule that cost the most time

**The loader skips a thunk whose IAT slot does not still hold the lookup
table's own hint/name RVA** — it reads that as an entry an earlier bind
already filled. A `.data` slot left at zero is therefore *silently* left at
zero, with no error. Seeding the slot with the ILT entry's value (what a real
IAT slot holds before snapping) makes the loader fill it. Proved by
binary-patching `FirstThunk` in a built `.exe`: seeded -> filled at every
`.data` address tried; zeroed -> filled at none.

## 44. Standalone toolchain re-verified at 179 (the note was 39 generations stale)

AGENTS.md had the toolchain verified at 140. Rebuilt it with 179:
`hybrid-compiler179.exe -r -t Windows-X86-64 -o
build/red-toolchain/windows-x64/red-toolchain-179.exe
red-toolchain-windows-hybrid.red` -> 7677952 bytes (140's was 7609344).
`--self-check` reports 276 resources. It compiles and runs a Red program with
`-r`, in dev mode, and dev mode survives the collector (200000 `append`s then
`collect`) -- so the AGENTS.md line "Dev mode is still not supported on
Windows" was stale; that was the collector bitmap-table bug, already fixed.
Cross-compiled `hello.red` to all four targets and *ran* each one: WSL
(1958904), armbian (1632576), Mac (1667280). On the Mac remember
`xattr -d com.apple.quarantine` after `scp`.

## 45. Mac suites re-verified at 179 (they were documented at 160)

Cross-compiled both macOS slices with 179 (driver: `build/tmp-mac179/build.sh`,
~18 min; 40 RS units then 23 Red units at ~33 s each) and ran them on the Mac
with `build/tmp-mac179/run.sh DIR LABEL`:

- 40/40 Red/System units, 0 failed assertions.
- 23/23 Red units, **10298 assertions**, 0 failures -- and every per-unit
  count is the one 160 reported.
- Darwin toolchain rebuilt at 179: `red-toolchain-179`, 6707072 bytes (175's
  was 6690656), `--self-check` 276, compiles a Red program natively in 10 s.

So 160 -> 179 changed nothing either suite can see.

## 46. 64-bit structlib builds committed (2c4283b18)

`system/tests/source/units/libs` only had 32-bit copies: `libstructlib.dylib`
is Mach-O i386, `libstructlib.so` is ELF i386. So `struct-test`/`size-test`
were *skipped* on macOS and Linux, never passing. Built from the checked-in
`structlib.c` with the host C compiler (it is portable: `stdint.h`,
`EXPORT extern` off MSVC):

- `structlib.dylib`    Mach-O 64-bit arm64  — `clang -arch arm64 -dynamiclib -fPIC`
- `structlib.so`       ELF 64-bit x86-64    — `gcc -shared -fPIC`
- `structlib-arm64.so` ELF 64-bit aarch64   — `gcc -shared -fPIC`

44 exports each. Tests still name `libstructlib.dylib` / `libstructlib.so`
(`#switch OS` at the bottom of struct-test.reds), so wiring them up is a
separate step — and on Linux a bare `dlopen` name needs `LD_LIBRARY_PATH`;
macOS found it in the cwd without help.

## 47. Linux-X86-64: struct-by-value is passed by reference and reordered

The probe that verified the new libs turned up a real backend bug.
`checkTriple8 [t [triple8! value] bias [integer!]]` on Linux-X86-64 emits

    mov $0x4,%edi          ; bias -> RDI
    lea 0x10(%rsp),%rsi    ; &t    -> RSI
    call checkTriple8

System V wants the 3 bytes in RDI and the bias in RSI. Results: 290 instead
of 10, and `checkBig` 0 instead of 1. Darwin-ARM64 10/1, Linux-ARM64 10/1,
Windows-X86-64 10/1 — so it is the System V classification in x64-codegen
only. Not yet fixed.

### Traps

- `run.sh` must invoke `./$f`. A bare `"$f"` makes the shell search PATH and
  every unit exits 127 -- which looks exactly like "the binaries are broken".
- `scp -r` drops the execute bit; `chmod +x` on the Mac side.
- The 40-unit macOS R/S slice is the 43-entry list in `run-red-system-tests.red`
  minus `struct-test`, `size-test` (need `structlib.dll`) and
  `dylib-auto-test` (generated with host absolute paths).

- `resolve-import-refs` in ELF.red patches *code* at every entry of an import's
  reference list, so a negative (data-slot) entry lands at the head of `.text`
  and corrupts the entry point. Symptom: SIGSEGV two bytes into `***_start`,
  with `49 89 e4 55` overwritten. PE and Mach-O already skipped non-code
  entries for the same reason.
- Comparing against a control binary (same program without the feature) is
  what localised it; `readelf -r` showed the relocation was correct all along.

## System V aggregate ABI — done (commits 850a06b9c, be9d19f87)

`struct-x64-test` is now the acceptance test on every 64-bit target.

- **The bug**: x64-codegen applied the *Win64* aggregate rule on every target
  (1/2/4/8 bytes -> one integer register, anything else by reference, both
  classes from one shared slot counter). System V splits an aggregate of <=16
  bytes into eightbytes and gives each an integer or a vector register by what
  it holds, from *independent* counters (6 GPR, 8 XMM); larger, or exhausted,
  goes to memory. Returns differ too: RAX then RDX and XMM0 then XMM1, each
  class from its own pair, MEMORY return on a hidden pointer in **RDI**.
- **The shape of the fix**: classify per eightbyte in
  `sysv-aggregate-eightbytes`; one helper, `sysv-claim-argument`, owns the
  register counting for *all five* boundaries (copy pass, argument loop,
  `plan-storage`, prologue, return path) so they agree by construction.
- **Two traps, caught only by assertion totals**: (1) the copy pass and
  `plan-storage` reserve an argument's stack slot **before** the claim counts
  it out, so `stack-offset` in the argument loop must come from that same
  pre-claim slot — using the running one put every register-missing argument
  8 bytes high (this was the last 5 failures); (2) a hidden return pointer
  takes the first integer register on both ABIs, so the caller's GPR counter
  starts at `hidden-shift`, not 0 (this was 29 of them).
- **Results at 182/184**: Windows-X86-64 621/621, Linux-X86-64 621/621 (was 5
  failures + an access violation), Linux-ARM64 627/628 (pre-existing;
  `x64-codegen/generate` accepts only ABI_WIN64/ABI_SYSV so ARM64 never
  reaches the new code). Red/System suite unchanged at 10593/12680/12680/0.
  Linux-X86-64 slice 40/40 compile, 40/40 run, 0 differed. Fixed point: 182
  vs 183 differ in 16 bytes, all build clock (PE timestamp, checksum, two
  `movabs rax` immediates, output file name, two `dd-Mmm-yyyy/h:mm:ss`).
- **structlib wiring**: `struct-x64-test.reds` now names the 64-bit builds
  (`structlib.dylib` on macOS; `structlib-arm64.so` vs `structlib.so` by
  `target` elsewhere; Windows keeps `structlib.dll`). Nested `#switch` inside
  `#switch` works in the Red/System preprocessor. `struct-test.reds` is the
  32-bit unit and keeps the i386 names.
- **Mac was unreachable all session** (`ssh macmini` -> banner exchange time
  out on 127.0.0.1:5588). Darwin-ARM64 is compile-verified only.

### Traps

- Instrumenting a failing `--test--` group with `print ["@N" lf]` before each
  assertion, then mapping markers back to source with a Python pass, is what
  localises individual failures — the runner only prints the group name.
- A compiler built from `red-bootstrap-*.red` must run from the repo root;
  unit tests that `#include %../../../../quick-test/quick-test.reds` must sit
  four levels below it.

## ARM64 stack slot width — fixed (commit 0ca8defd6)

`struct-x64-test` is now clean on every runnable 64-bit target.

- **The bug**: AAPCS64 gives every stack argument a whole eight-byte slot,
  but `abi-parameter-location` (arm64-codegen.reds) advanced the offset by
  the natural `width` — 4 for `integer!`/`float32!`. `checkBigOverflow 1 2 3
  4 5 6 7 s3 8 42` (seven ints fill x0-x6, so the 16-byte `big!` and both
  trailing ints spill) wrote `marker` at sp+20; gcc puts it at **sp+24**.
- **How it was found**: instrumented the failing group with `print ["@N"]`
  before each assertion — `--assert` is a *function*, so quick-test prints
  `FAILED` immediately, and the marker right before it names the assertion.
  (Assertions 51+ live inside helper *functions*, so their markers repeat;
  that is not a re-run.) Then objdump'd the call site against `gcc -O2 -c`
  for the same C call.
- **The fix**: advance by `align size 8`, align to `max(alignment, 8)`.
  `abi-trailing-argument` already did exactly this and its comment stated
  the rule — the fixed-parameter path just did not follow it.
- **Why only the native boundary noticed**: both sides of a
  Red/System-to-Red/System call ask the same function, so internal calls
  were self-consistently wrong.
- Linux-ARM64 627/628 -> **628/628**; its 40-unit suite still 0 failed
  assertions; Windows Red/System suite still 12680/12680/0. Fixed point:
  185 vs 186 differ in 18 bytes of build clock.

### Traps

- `ssh macmini` is a **local port-forward** (127.0.0.1:5588). When it fails
  with "banner exchange timed out", check `netstat` — if nothing listens on
  5588 the tunnel is down and there is no way to bring it up from here.
- To attribute a quick-test failure, put `print ["@N" lf]` before each
  `--assert`; the FAILED line prints immediately after the guilty marker.

## struct-x64-test is now a standing Linux gate (commit dd6907cf7)

- `build/linux-hybrid/rs-suite-linux.sh` runs **41** units now: added
  `struct-x64-test` and made the driver ship the 64-bit `structlib.so` /
  `structlib-arm64.so` for its target with `LD_LIBRARY_PATH=/tmp`. At 185 both
  Linux targets are 41/41 compile, 41/41 run, every unit 0 failed assertions
  (struct-x64-test 621/621 x86-64, 628/628 ARM64).
- **The `differed: 0` the driver printed was vacuous.** Its reference is
  `build/win-regression/rs-<gen>/`, which only exists up to gen 98, so
  `[ -f ... ]` was false and every unit fell through to `OK`. It now prints
  `NOREF` and counts them separately. Trust the compile/run counts and each
  unit's own `Number of Assertions Failed`, not the diff count.

### Traps

- Python `open(p,'w')` on Windows translates `\n` to `\r\n`. Writing a shell
  script that way rewrites the whole file CRLF and turns a 27-line diff into a
  137-line one; reopen in `'wb'` and strip `\r\n` afterwards.

## De-risking Darwin-ARM64 without the Mac (commit d5b8e3af7)

`arm64-codegen.reds` is shared by Linux-ARM64 and Darwin-ARM64, and the Mac
tunnel was down, so the ARM64 stack-slot change was verified against **Red**
rather than trusted: 184 vs 185 emit different bytes for `parse-test`,
`object-test`, `series-test`, `function-test` on Linux-ARM64 (a call with >8
args now reserves a wider outgoing area, so frame offsets move), but all four
run to identical totals with 0 failures on armbian -- parse 1518, object 658,
series 1119, function 147, the same numbers the Mac reported at 179.

- Lesson: when a shared backend cannot be run on one target, **diff the two
  generations' output and then run it on a sibling target**. Identical byte
  output proves inertness; different bytes plus identical run totals still
  proves behavioural inertness, which is what actually matters.

## Red suite on Linux-x86-64 at 185 (commit 56f604549)

The System V aggregate change had only been measured against Red/System, which
is not what drives a native call from the runtime. `JOBS=4 bash
build/linux-hybrid/red-suite-linux.sh hybrid-compiler185.exe Linux-X86-64 wsl`:
**57/57 compile, 57/57 clean, 57 exit-0, 16854 assertions, 0 failed**, and every
per-unit count matches the Mac's at 179 (logic 95, integer 1760, float 1793,
series 1119, parse 1518, lexer 929...). Takes ~16 min. This is the System V
change's real regression gate.

- Lesson: a codegen ABI change needs a Red-level gate, not just a
  Red/System-level one -- Red/System exercise the convention against itself,
  Red exercises it against the runtime's C-calling routines.

## Toolchain re-verified at 185 (commit 24ce124ab)

`red-toolchain-185.exe` 7688704 bytes, `--self-check` 276 resources, Red
hello.red compiles with `-r` (1970688) and in dev mode (128512) and prints in
both, Red/System program compiles and runs. ~2 min to build; worth re-running
after any codegen change since it is the only build reaching some sites.

### Traps

- `build/linux-hybrid/hello.reds` is `main: does [print ...]` -- a **no-op**.
  In Red/System the entry point is the top level, so `main` is never called
  and the program prints nothing. It proves the link, not the run. Use a
  top-level `print` to check a Red/System program actually runs.

## Measuring a change on an unreachable target (commit 279feb8a5)

Mac unreachable, so instead of guessing what the ARM64 stack-slot change does
to Darwin-ARM64, measure it with a **control**:
- `red-toolchain-darwin-hybrid.red` via 184 vs 185: same 6723488 bytes,
  **5911 differing bytes** in 691 runs, all frame offsets/immediates.
- Same pair on `red-toolchain-windows-hybrid.red` (unreachable by the change):
  **16 bytes** — the build-clock noise floor.

So Darwin-ARM64 codegen *does* change, in exactly the narrow way Linux-ARM64
does (>8-arg calls reserve a wider outgoing area). Apple ARM64 follows AAPCS64
for non-variadic stack args, so the wider slots are right there too.

- Lesson: when you cannot run a target, diff two generations' output **and
  diff a control target the change cannot reach**. Without the 16-byte control,
  5911/6.7MB would be indistinguishable from noise.

- Red suite on **Linux-ARM64** at 185: 57/57 compile, 57/57 clean, 57 exit-0,
  16854 assertions, 0 failed -- and a per-unit diff against the Linux-X86-64
  run is empty. Both drivers must use a target-specific OUT dir
  (`red-<compiler>-x86-64` / `red-<compiler>-arm64`) or the two runs overwrite
  each other's logs.

- Red compiler tests at 185: **321 assertions, 319 passed, 2 failed, 30
  compile-failures** (was 262/251/12 at 140). The 30 compile-failures are
  bookkeeping -- those tests feed the compiler deliberately broken snippets.
  The 2 real failures are both in regression-test-redc-5.red and neither is a
  compiler defect:
  * **#4190** `make face! [...]` dies as `undefined word face!`. `face!` lives
    in modules/view/view.red, so it needs `Needs: [View]`; the harness's
    `--compile-and-run-this-red` only prepends a bare `Red []`. Compiled with
    `Needs: [View]` it prints `boom` and the test passes.
  * **#4613** asserts `"bug$0" = trim qt/output`, but the program prints
    `BUG$0.00`. Upstream asserted `bug$0 = load qt/output` -- money! equality,
    where the currency *word* is case-insensitive and 0 == 0.00 -- so upstream
    passes on the same text. The Red port cannot write the `bug$0` literal (the
    harness rejects an unregistered currency at load), so it replaced a value
    comparison with a stricter text one. Restoring the semantics means
    `"bug$0.00" = lowercase trim qt/output`.

- Pre-existing, unrelated: any `Red [Needs: [View]]` program on Windows prints
  `*** Error in GetClassInfoEx` at startup, even `print "hi"`. View otherwise
  works (the face is made, `fc/parent` is set). Not chased.

- Correction to the #4613 note above: `lowercase trim qt/output` is **not**
  enough. Plain `trim` re-appends the line feed it removed --
  `trim-head-tail` in runtime/datatypes/string.reds sets `append-lf?` and
  re-pokes it when neither `/head` nor `/tail` was given -- so
  `trim "BUG$0.00^/"` is `"BUG$0.00^/"`. Use `trim/tail` or `trim/all`.
  Committed as `"bug$0.00" = lowercase trim/tail qt/output`; the suite is now
  321 assertions / 320 passed / 1 failed, the one being #4190 (needs View).

- Two debugging traps, both of which cost a full 8-minute suite run each:
  * `lowercase` mutates its argument **in place**. Printing `mold qt/output`
    after a `lowercase trim qt/output` shows the lowercased string and points
    you at the wrong assertion.
  * `--assert` in qt-runner prints only `FAILED: <test-name>`, once per failing
    assertion, and the runner captures the compiler's stdout instead of echoing
    it -- so a successful compile's `comp-output` is invisible in the log. To
    locate a failure inside a `--test--`, put `print ["@N" mold <expr>]` before
    each assertion and read the markers off in order.

- Red/System **compiler** tests at 185: **124/124**, 0 failed, 70
  compile-failures -- unchanged from 150. Reads the compiler from
  **`RED_SYSTEM_COMPILER`**, not `RED_COMPILER`; wrong name = 300ms exit that
  looks like a crash.

- Both "still open" items in the bootstrap-chain header of AGENTS.md are
  already fixed elsewhere in the file and the header copy is stale: the 32-byte
  `#import` name was a `binary/convert` /part bug (fixed at 169, not
  RSIR_IMPORT_SIZE as the header says) and the dev-mode access violation was
  the collector's bitmap-table choice (fixed, verified at 179). Trust the
  "Fixed:" bullets over the header narrative.

- View headless suite at 185 (`red-console.exe
  tools/self_hosting/run-red-view-headless-tests.red`, RED_COMPILER set):
  **148 tests / 246 assertions / 246 passed / 0 failed** -- unchanged from 152.
  Under a minute, and it is the cheap gate for the *Win64* side of an
  `x64-codegen` change, since the SysV arms are all `target-abi = ABI_SYSV`
  and Windows-only edits need something that actually runs here.

- **Windows View is broken, pre-existing**: `Red [Needs: [View]] view/no-wait
  [button "OK"]` prints `*** Error in GetClassInfoEx` then `*** View Error:
  CreateWindowEx failed!` per widget and segfaults. Identical at 184 and 185
  (same output size), so not the SysV/ARM64 work. Probe
  `build/tmp-imp/gci3.reds`: all 9 system classes resolve via
  GetClassInfoExW; RegisterClassExW returns an atom with GetLastError 0 but the
  class is invisible to the next GetClassInfoExW (hInstance or null);
  CreateWindowExW returns null with GetLastError 998 (ERROR_NOACCESS). 1- and
  3-arg imports in the same program are fine -- chase the 12-arg call first.

- Gotcha for scratch Red/System probes: `handle!` does not exist (only Red
  programs define it) -- `declare handle!` fails with "not found or not
  supported" and a `[handle!]` parameter leaves the symbol undefined, which
  surfaces as a confusing `unsupported type` error at the internal
  `***-normal-exit`. Use `int-ptr!`. Also, passing a struct-typed parameter to
  an import (`lpwcx [WNDCLASSEX]`) fails the same internal way; declare it
  `[byte-ptr!]` and pass `as byte-ptr! wcex`.

- Correction/refinement: the Windows View breakage is **not a regression** --
  133, 184 and 185 all emit byte-identical 2716672-byte output and fail the
  same way. It has never worked here. Don't bisect. Fork's View coverage is the
  headless (246/246) and terminal backends; the Windows GUI backend is
  unexercised territory.

- Arity is NOT the cause: a scratch ladder proves 14-argument imported calls
  work (`CreateFontW` returns an HFONT, `TextOutW` (5) returns 1,
  `CreateCompatibleDC` (1) works). So `CreateWindowExW` failing with
  ERROR_NOACCESS is something else -- most likely the class never really
  registered, or the wndproc returning 0 for WM_NCCREATE (my probe's WndProc
  did return 0, which alone makes CreateWindowEx fail).

- Baseline advanced to **187** (186->187, 6419456 bytes, same size as 185 and
  186). Fixed point: 186 vs 187 differ in **19 bytes** (185 vs 186 was 18 --
  the count moves by one whenever the variable-length `dd-Mmm-yyyy/h:mm:ss`
  build date does). Offsets: 137/138, 217/218 (PE header), 313939-41 and
  319343-45 (embedded output file name and date), plus three clusters near the
  end (5724235, 6028913-18, 6302441-45) that are the two `movabs rax`
  immediates and the second date. No source changed between 185 and 187, only
  the ported #4613 assertion, so every 185 measurement holds at 187.

- Standalone toolchain re-verified at **187**: `red-toolchain-187.exe`
  7688704 bytes (identical to 185), `--self-check` 276 resources, `hello.red`
  1971200 with `-r` and 129024 in dev mode, both printing. Cross-compiles and
  runs on Linux-X86-64 (1958784, WSL) and Linux-ARM64 (1632496, armbian) --
  the toolchain end-to-end over both ABI changes. Darwin-ARM64 cross-compiles
  but cannot be run.

- `red-toolchain-187.exe -r -o build/tmp-imp/gui-console.exe
  environment/console/GUI/gui-console.red` compiles in 52s to **3319296
  bytes** (`GUI backend: native`, `Modules: View JSON CSV`) -- but the console
  then dies on the same `*** Error in GetClassInfoEx` + three
  `CreateWindowEx failed!` lines. So the GUI console builds on Windows and has
  never been able to open a window here. Same pre-existing gap as any View
  program; the fork's working View backends are the headless and terminal ones.

- Windows View failure localized much further (temporary prints in
  modules/view/backends/windows/gui.reds, reverted). It is **not the call**:
  * `CreateFontW` (14 args) succeeds when called from inside the failing Red
    program, one line above the CreateWindowEx that fails -- so not arity.
  * A **minimal** `CreateWindowEx 0 #u16 "STATIC" null 0 0 0 0 0 null null
    null null` still fails with 998, and STATIC is a class Windows always has.
  * Ruled out: struct-typed import params, handle! vs int-ptr!, NUL
    termination (to-utf16-len does write the terminator), pointer validity
    (our own code reads correct UTF-16 from both), literals vs variables, and
    every argument value.
  * So: look at process/thread state in `init` (enable-visual-styles, DX-init)
    before the first window.
- Trap: **the toolchain cannot see edits to `modules/`** -- it embeds
  build/generated/red-toolchain-resources.generated.red. Instrumenting the
  backend requires hybrid-compilerN.exe; red-toolchainN.exe silently produced
  byte-identical output and my prints never appeared.

## Windows View / gui-console: root cause found and fixed (189/190)

Two independent blockers, both fixed. `build/tmp-imp/gui-console.exe` (built
by `red-toolchain-190.exe -r`, 3319296 bytes) now opens a window and runs.

1. **UTF-16 literals were emitted at odd addresses.** `#u16` was interned as
   `[array byte N 1]`; an inline array takes its alignment from its element
   width (`layout-type`, kind = -7), so alignment was 1 and one odd-length
   predecessor left the next literal odd. The kernel probes every `WCHAR*`
   with its own alignment -> STATUS_DATATYPE_MISALIGNMENT -> user32 reports
   **ERROR_NOACCESS (998)**, which is why `RegisterClassExW`/`CreateWindowExW`
   failed. Proof: `build/tmp-imp/args27.reds` registers the *same* bytes from
   an even heap address (ok) and from that buffer `+1` (998).
   Fix: `add-static-bytes/wide` in compiler/rsir-frontend.red interns as
   `intern-array -4 ((length? data) / 2) 2`; the bytes-initializer check in
   system/codegen/x64-codegen.reds (site 303) and arm64-codegen.reds (site 27)
   now accepts element width 2, not just 1.
   Earlier "it's the stack" hypothesis is dead: the PE commits all 8 MB, every
   call site is 16-byte aligned, and cdb catches no AV/SOV/SBO.

2. **The Windows View backend never called `GdiplusStartup`.** Every GDI+ call
   returned GdiplusNotInitialized and left its handle null, so
   `GdipSetStringFormatAlign` dereferenced zero and `size-text` segfaulted.
   Fix: `init-gdiplus` in modules/view/backends/windows/gui.reds, called from
   `init`; imports added to win32.reds.

Verification at 190: fixed point holds (190 vs 191 differ in 17 bytes of
build-clock noise); Red/System suite 12680/12680; headless View 246/246;
`v3.red` prints `window-and-widgets-created` where the 187 build printed three
`CreateWindowEx failed!` and segfaulted.

Remaining, cosmetic: gui-console prints two `Math Error: attempt to divide by
zero` at startup -- `view/flags/no-wait win [resize]` fires `on-resizing`
before `terminal/update-cfg` measures the font, so `adjust-console-size`
divides by a still-zero `char-width`/`line-h`. `update-cfg` runs right after
and fixes them, so the console is fine once up. App-level ordering wart.

Trap: after editing `modules/`, regenerate
`build/generated/red-toolchain-resources.generated.red`
(hybrid-compilerN -r -o build/tmp/gen-res.exe
 tools/self_hosting/generate-toolchain-resources.red, then
 gen-res.exe E:/temp3/red build/generated/red-toolchain-resources.generated.red)
or the standalone toolchain keeps shipping the old copy.
